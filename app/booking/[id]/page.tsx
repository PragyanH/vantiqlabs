import Link from 'next/link';import BookingFlow from '@/components/booking/BookingFlow';import {createPublicServerSupabaseClient} from '@/lib/supabase';import {type AlumniProfileWithCollege} from '@/lib/types'
export default async function BookingPage({params}:{params:{id:string}}){const {data,error}=await createPublicServerSupabaseClient().from('alumni_profiles').select('*, colleges(name, slug)').eq('id',params.id).eq('is_published',true).maybeSingle();if(error)console.error('Unable to load booking mentor:',error.message);return data?<BookingFlow mentor={data as AlumniProfileWithCollege}/>:<main className="booking"><h1>{error?'We couldn’t load this mentor. Please try again.':'Mentor not found'}</h1><Link className="btn" href="/browse">Browse mentors</Link></main>}
