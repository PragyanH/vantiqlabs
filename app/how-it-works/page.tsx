'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation'
export default function HowItWorks(){const [started,setStarted]=useState(false),r=useRouter();const items=[['01','Discover','Find your mentor','Match with alumni filtered by your college, target company, and the path you want to explore.'],['02','Learn','Follow proven playbooks','Use practical preparation plans shaped by people who have already navigated the same decisions.'],['03','Connect','Book focused guidance','Bring your real questions to a focused conversation with someone who understands the role you want.'],['04','Accelerate','Turn advice into progress','Convert clear feedback into smaller next steps, stronger proof, and visible career momentum.']];return <main className="how-page"><section className="how-hero"><span>HOW VANTIQ WORKS</span><h1>Four moves from uncertainty<br/>to career momentum.</h1><p>Trusted alumni, lived playbooks, focused conversations, and visible progress.</p></section><section className={started?'journey-stage is-open':'journey-stage'}><div className="journey-orbit"><div className="journey-disc">{items.map(([n,h,d],i)=><article className={'journey-piece piece-'+(i+1)} key={n}><span>{n}</span><b>{h}</b><small>{d}</small></article>)}<button className="journey-trigger" onClick={()=>setStarted(true)}><span>{started?'Replay':'Click me'}</span><i>→</i></button></div><div className="journey-callouts">{items.map(([n,h,,description],i)=><article className={'journey-callout callout-'+(i+1)} key={n}><span>{n}</span><div><b>{h}</b><p>{description}</p></div></article>)}</div></div></section><section className="how-close"><span>START WHERE YOU ARE</span><h2>Your next step does not need<br/>to be another guess.</h2><button className="dark-cta" onClick={()=>r.push('/browse')}>Find your mentor →</button></section></main>}
