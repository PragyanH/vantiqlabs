'use client'
// Client component: profile tab state.
import {useState} from 'react';import {type Mentor} from '@/lib/types';import LockedContent from './LockedContent'
export default function ProfileTabs({mentor}:{mentor:Mentor}){const [tab,setTab]=useState('Overview');return <section><div className="tabs">{['Overview','Career roadmap','Interview prep','Resources'].map(x=><button key={x} onClick={()=>setTab(x)} className={tab===x?'active':''}>{x}</button>)}</div>{tab==='Overview'?<><h2>Make your next move with clarity.</h2><p className="profile-text">{mentor.name.split(' ')[0]} helps students turn uncertainty into a focused career plan.</p><div className="info-grid"><div><span>EXPERTISE</span><p>{mentor.expertise.join(' · ')}</p></div><div><span>ALUMNI NETWORK</span><p>{mentor.college}</p></div></div></>:<LockedContent tab={tab}/>}</section>}
