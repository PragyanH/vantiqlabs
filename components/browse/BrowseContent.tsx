'use client'
// Client component: search and mentor grid.
import {useMemo,useState} from 'react';import {mentors} from '@/lib/types';import MentorCard from '@/components/ui/MentorCard'
export default function BrowseContent({college}:{college:string}){const [query,setQuery]=useState('');const filtered=useMemo(()=>mentors.filter(m=>(college==='All colleges'||m.college===college)&&`${m.name} ${m.role} ${m.company} ${m.expertise.join(' ')}`.toLowerCase().includes(query.toLowerCase())),[college,query]);return <section className="browse-content"><div className="browse-heading"><div><p className="section-kicker">VERIFIED PEOPLE, PRACTICAL ADVICE</p><h1>Explore mentors</h1></div><span>{filtered.length} mentors</span></div><div className="search-wide"><span className="icon">⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search by name, role, company, or skill"/><button onClick={()=>setQuery('')}>Clear</button></div><div className="chip-row"><button className="chip active">All expertise</button><button className="chip">Engineering</button><button className="chip">Product</button></div>{filtered.length?<div className="mentor-grid all">{filtered.map(m=><MentorCard key={m.id} mentor={m} href={'/profile/'+m.id}/>)}</div>:<div className="empty"><b>No mentors match that search.</b></div>}</section>}
