'use client';import {useRouter} from 'next/navigation';import MentorCard from '@/components/ui/MentorCard';import {type AlumniProfileWithCollege} from '@/lib/types'
export default function DashMain({name,mentors,error}:{name:string;mentors:AlumniProfileWithCollege[];error?:string}){const r=useRouter();return <section className="dash-main"><p className="section-kicker">THURSDAY, 17 JULY</p><h1>Good morning, {name}. <em>Keep going.</em></h1><div className="goal"><div><span>YOUR CAREER GOAL</span><h2>Become a product engineer</h2><p>3 of 5 milestones completed</p></div><b>60%</b></div><div className="dash-head"><h2>Recommended for you</h2><button className="text-btn" onClick={()=>r.push('/browse')}>Explore all →</button></div>{error&&<div className="empty"><b>{error}</b></div>}<div className="mentor-grid">{mentors.map(m=><MentorCard key={m.id} mentor={m} href={'/profile/'+m.id}/>)}</div></section>}
