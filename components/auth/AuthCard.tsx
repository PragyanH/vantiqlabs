'use client'
// Client component: auth form and appearance toggle.
import Link from 'next/link';import {useRouter} from 'next/navigation';import {useState} from 'react'
export default function AuthCard({mode}:{mode:'signin'|'signup'}){const r=useRouter(),[theme,setTheme]=useState<'light'|'dark'>('dark'),signup=mode==='signup';return <main className={'auth-page '+theme}><button className="auth-back" onClick={()=>r.back()}>←</button><div className="auth-brand">✦ VantiqLabs</div><button className="theme-toggle" onClick={()=>setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?'☀':'☾'}<span>{theme==='dark'?'Light':'Dark'} mode</span></button><section className="auth-card"><div className="auth-kicker">{signup?'START YOUR VANTIQ JOURNEY':'WELCOME BACK'}</div><h1>{signup?'Create your account':'Sign in to VantiqLabs'}</h1><p>{signup?'Already have an account?':'New to VantiqLabs?'} <Link href={signup?'/auth/signin':'/auth/signup'}>{signup?'Sign in':'Create an account'}</Link></p><form>{signup&&<label>Full name<input placeholder="Enter your full name"/></label>}<label>College email<input type="email" placeholder="you@college.edu"/></label><label>Password<input type="password" placeholder="Enter your password"/></label><button className="auth-submit" type="button">{signup?'Create your account':'Sign in to your account'} →</button></form><div className="auth-divider"><span>OR CONTINUE WITH</span></div><button className="social-auth"><b className="google-g">G</b>Continue with Google</button></section></main>}
